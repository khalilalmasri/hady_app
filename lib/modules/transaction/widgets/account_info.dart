import 'package:flutter/material.dart';

class AccountInfoTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Account information'));
  }
}